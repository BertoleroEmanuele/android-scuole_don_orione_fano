<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bertolero.scuoledonorionefano">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".calendario.ArchivioEventi"></activity>
        <activity android:name=".circolari.CircolareReader" />
        <activity android:name=".notizie.WebViewNotizia" />
        <activity android:name=".contatti.ActivityContatti" />
        <activity
            android:name=".video.VideoActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            />
        <activity android:name=".orientamento.OrientamentoActivity" />
        <activity android:name=".circolari.CircolariActivity" />
        <activity android:name=".calendario.CalendarioActivity" />
        <activity android:name=".notizie.NotizieActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/HomePageStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>